//Write a function that takes a string (a) as argument. Remove the first 3 characters of a. Return the result//

// Escribe una funcion
// que tome una cadena (a) como argumento
// Elimina los primeros 3 caracteres de a
//Retorna el resultado

function myFunction(a){

return a.slice(3)
}
 
console.log(myFunction("facundo"))