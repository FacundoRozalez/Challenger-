//Write a function that takes a string (a) and a number (n) as argument. Return the nth character of 'a'.//

// Escribe una funcion
// que tome una cadena (a) y un numero (n) como argumento
//Retornar el enesimo caracter de 'a'


function myFunction(a, n){

    return a[n-1]
}

console.log(myFunction("facundo",3))